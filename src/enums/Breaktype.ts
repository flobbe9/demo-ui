/**
 * Enum defining a type of break in a word document.
 * 
 * @since 0.0.5
 */
export enum BreakType {
    /** standard line break */
    TEXT_WRAPPING = "TEXT_WRAPPING", 
    COLUMN = "COLUMN",
    PAGE = "PAGE"
}